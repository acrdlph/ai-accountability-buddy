---
phase: 01-core-voice-agent
plan: 02
type: execute
wave: 2
depends_on: ["01-01"]
files_modified: []
autonomous: false
requirements: [FR3, FR5, FR6, FR9, NFR1, NFR2]

must_haves:
  truths:
    - "Agent worker starts and connects to LiveKit Cloud without errors"
    - "Dispatching via CLI creates a room and the agent joins it"
    - "Agent speaks first within 2 seconds of a participant joining — no silence"
    - "Agent's tone is direct and no-nonsense — does not hedge, apologize, or soften"
    - "Saying goodbye causes the agent to call end_call and the room is deleted cleanly"
    - "Voice responses feel natural with no perceptible lag"
  artifacts: []
  key_links:
    - from: "agent.py on_enter"
      to: "browser audio output"
      via: "generate_reply produces speech within 2 seconds"
      pattern: "agent speaks first"
    - from: "agent.py end_call tool"
      to: "room deletion"
      via: "user says goodbye -> LLM calls end_call -> room deleted"
      pattern: "room.*deleted"
---

<objective>
Verify the Phase 1 voice agent works end-to-end in the browser: agent speaks first, conversation flows naturally with tough-love tone, and the call terminates cleanly.

Purpose: Confirm all Phase 1 requirements are met before moving to Phase 2 (telephony). Browser testing validates voice quality, latency, personality, and termination without needing a phone.
Output: Verified working agent, ready for Phase 2 SIP integration
</objective>

<execution_context>
@/Users/achill/.claude/get-shit-done/workflows/execute-plan.md
@/Users/achill/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-core-voice-agent/01-RESEARCH.md
@.planning/phases/01-core-voice-agent/01-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Start agent worker and verify connection to LiveKit Cloud</name>
  <files></files>
  <action>
1. Verify `.env.local` has real credentials (not placeholder values). If placeholders remain, halt and inform the user they need to fill in real LIVEKIT_URL, LIVEKIT_API_KEY, LIVEKIT_API_SECRET, and OPENAI_API_KEY before proceeding.

2. Start the agent worker:
   ```bash
   cd /Users/achill/accountability-buddy && uv run agent.py dev
   ```
   This runs in the foreground. Watch logs for:
   - "registered worker" or similar connection success message
   - No import errors, no credential errors, no model file missing errors

3. If the worker connects successfully, it will wait for dispatch. Keep it running.

4. In a second command, dispatch the agent:
   ```bash
   lk dispatch create --new-room --agent-name accountability-buddy --metadata '{}'
   ```
   Watch for: room created, agent joining the room.

Note on `agent_name` (from research pitfall #1): Because `agent_name="accountability-buddy"` is set in WorkerOptions, the agent does NOT auto-dispatch. The explicit `lk dispatch create` command is required. If the dispatch returns "agent not found", verify livekit.toml matches the agent_name in WorkerOptions.
  </action>
  <verify>
    <automated>cd /Users/achill/accountability-buddy && timeout 15 uv run agent.py dev 2>&1 | head -20 || true</automated>
  </verify>
  <done>Agent worker starts without errors, connects to LiveKit Cloud, and responds to dispatch commands. Room is created and agent joins.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Browser verification of full voice agent behavior</name>
  <files></files>
  <action>
Human verification checkpoint. Present the verification checklist below to the user and wait for approval.
  </action>
  <verify>User confirms all checklist items pass by typing "approved"</verify>
  <done>All Phase 1 requirements verified by human in browser: agent speaks first (FR6), tough-love tone (FR5), natural conversation (FR3), low latency (NFR1), clean audio (NFR2), clean termination (FR9).</done>
  <what-built>Complete Phase 1 voice agent: AccountabilityAgent with tough-love personality, agent-speaks-first behavior, natural conversation flow via OpenAI Realtime, and clean call termination via end_call tool with room deletion.</what-built>
  <how-to-verify>
With the agent worker running (`uv run agent.py dev`) and dispatched (`lk dispatch create --new-room --agent-name accountability-buddy --metadata '{}'`):

1. **Open LiveKit Playground** at https://agents-playground.livekit.io
2. **Connect** using the same LiveKit Cloud project credentials (the playground should auto-detect the project from LIVEKIT_URL)
3. **Join the room** that was created by the dispatch command

Verify each requirement:

**FR6 -- Agent Speaks First:**
- [ ] Within 1-2 seconds of joining, the agent greets you -- no awkward silence
- [ ] The greeting is direct and starts the check-in immediately (no small talk)

**FR5 -- Tough-Love Personality:**
- [ ] Agent tone is direct and firm -- not passive, apologetic, or overly supportive
- [ ] When you mention something you did not do, the agent challenges you on it
- [ ] When you mention something you completed, acknowledgment is brief (not effusive praise)

**FR3 -- Conversational Check-In:**
- [ ] Conversation flows naturally -- agent asks questions, responds to your answers
- [ ] No robotic or stilted phrasing

**NFR1 -- Latency:**
- [ ] Voice responses feel natural -- no perceptible lag between your speech and the agent's reply
- [ ] Conversation feels like a phone call, not a chat with delays

**NFR2 -- Audio Quality:**
- [ ] Agent voice is clear, no static or artifacts
- [ ] Your voice is heard clearly by the agent (it responds appropriately to what you say)

**FR9 -- Graceful Termination:**
- [ ] When you indicate the conversation is done (e.g., "that's it", "we're done"), the agent says a direct goodbye
- [ ] After the goodbye, the agent calls end_call -- the room is deleted and the connection ends
- [ ] The goodbye message plays fully (not cut off mid-sentence)
- [ ] No lingering connections after room deletion
  </how-to-verify>
  <resume-signal>Type "approved" if all checks pass, or describe specific issues (e.g., "agent doesn't speak first", "tone is too soft", "goodbye gets cut off")</resume-signal>
</task>

</tasks>

<verification>
All Phase 1 success criteria from ROADMAP.md:
1. Running `python agent.py dev` starts the agent worker and connects to LiveKit Cloud without errors
2. Opening the LiveKit browser playground and joining the room causes the agent to speak first within 2 seconds
3. The agent's tone is direct and no-nonsense: it does not hedge, apologize, or soften its assessments
4. After the conversation ends naturally, the agent says goodbye and the room is deleted cleanly
5. Voice responses feel natural with no perceptible lag (OpenAI Realtime single-hop speech-to-speech)
</verification>

<success_criteria>
- Agent worker starts and connects without errors
- Human confirms: agent speaks first (FR6), tough-love tone (FR5), natural conversation (FR3), low latency (NFR1), clean audio (NFR2), clean termination (FR9)
- Phase 1 is complete and ready for Phase 2 (Twilio SIP integration)
</success_criteria>

<output>
After completion, create `.planning/phases/01-core-voice-agent/01-02-SUMMARY.md`
</output>
