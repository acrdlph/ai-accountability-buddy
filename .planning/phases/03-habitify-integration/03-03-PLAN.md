---
phase: 03-habitify-integration
plan: 03
type: execute
wave: 3
depends_on: [03-02]
files_modified: []
autonomous: false
requirements: [FR2, FR4]

must_haves:
  truths:
    - "At call start, the agent correctly names each habit due today and knows which are already marked complete"
    - "When the user says a habit is done, Habitify reflects completed status immediately after the call"
    - "For goal-based habits, Habitify logs the numeric value — not just a yes/no status"
    - "Habits the user says they skipped are correctly left as incomplete in Habitify"
    - "The agent handles at least 5 habits in a single call without tool-step limits blocking completion"
  artifacts: []
  key_links:
    - from: "agent.py entrypoint"
      to: "habitify_briefing.py"
      via: "Pre-call briefing injected into agent instructions"
      pattern: "generate_briefing"
    - from: "agent.py MCPServerHTTP"
      to: "https://mcp.habitify.me/mcp"
      via: "Voice agent writes completions during call"
      pattern: "mcp.habitify.me"
---

<objective>
End-to-end verification of the complete Habitify integration: live call with real habit data, verify pre-call briefing, habit discussion, and write-back to Habitify.

Purpose: Validate that all Phase 3 success criteria are met with real Habitify data over a real phone call. This is the proof that FR2 (habit awareness) and FR4 (automatic tracking) work in production conditions.

Output: Verified E2E flow, any bug fixes if needed.
</objective>

<execution_context>
@/Users/achill/.claude/get-shit-done/workflows/execute-plan.md
@/Users/achill/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-habitify-integration/03-01-SUMMARY.md
@.planning/phases/03-habitify-integration/03-02-SUMMARY.md
@agent.py
@habitify_briefing.py
@habitify_auth.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Start agent and trigger test call</name>
  <files></files>
  <action>
Run the agent in dev mode and trigger a test call:

1. Start the agent:
   ```bash
   cd /Users/achill/accountability-buddy && uv run python agent.py dev
   ```
   Watch the logs for:
   - "Habitify access token refreshed successfully" — token refresh worked
   - "Pre-call briefing generated (N chars)" — briefing was produced
   - Any errors from the MCP connection or briefing generation

2. In a separate terminal, dispatch a call:
   ```bash
   lk dispatch create \
     --new-room \
     --agent-name accountability-buddy \
     --metadata '{"phone":"'$DEFAULT_PHONE_NUMBER'"}'
   ```

3. Watch the agent logs during the call for:
   - MCP tool discovery messages (tools loaded from Habitify MCP server)
   - Tool call logs when the agent marks habits complete
   - Any errors or "max tool steps reached" warnings

Note: If the briefing or MCP connection fails, check:
- HABITIFY_CLIENT_ID and HABITIFY_REFRESH_TOKEN are set in .env.local
- Token refresh is working (run `uv run python -c "import asyncio; from habitify_auth import refresh_habitify_token; print(asyncio.run(refresh_habitify_token())[:20])"`)
- MCP server is reachable (the SSE endpoint at https://mcp.habitify.me/mcp)

If any issues are found, fix them in the relevant files before proceeding to the checkpoint.
  </action>
  <verify>
    <automated>cd /Users/achill/accountability-buddy && uv run python -c "
import asyncio
from habitify_auth import refresh_habitify_token
from habitify_briefing import generate_briefing

async def test():
    token = await refresh_habitify_token()
    print(f'Token OK: {token[:20]}...')
    briefing = await generate_briefing(token)
    print(f'Briefing OK ({len(briefing)} chars):')
    print(briefing[:200])

asyncio.run(test())
"</automated>
  </verify>
  <done>
    - Token refresh succeeds
    - Pre-call briefing generates with real habit data
    - Agent starts without errors
    - Test call dispatched successfully
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Verify live call with Habitify integration</name>
  <files></files>
  <action>
Human verifies the complete Habitify integration over a live phone call:

1. Answer the phone when it rings
2. Verify the agent mentions SPECIFIC habits by name (not generic "how was your day")
3. Verify the agent references patterns if applicable (e.g., streaks, recent completions)
4. Tell the agent you completed at least one habit — the agent should use the MCP tool to mark it
5. Tell the agent you skipped at least one habit — the agent should NOT mark it
6. For a goal-based habit (if you have one), give a specific number — the agent should log the value
7. After the call ends, open Habitify and verify:
   - Habits you said you completed show as "completed"
   - Habits you said you skipped are unchanged (not marked)
   - Goal-based habits show the correct numeric value
8. Check the agent logs for any "max tool steps reached" warnings
  </action>
  <verify>
    <automated>echo "Human verification required — no automated check for live call quality"</automated>
  </verify>
  <done>
    - Agent names specific habits from Habitify at call start
    - Agent references multi-day patterns (streaks, slacking) if applicable
    - Completed habits reflected in Habitify after call
    - Skipped habits left as-is in Habitify
    - Goal-based habits logged with numeric values
    - No "max tool steps reached" warnings in agent logs
  </done>
</task>

</tasks>

<verification>
All Phase 3 success criteria from ROADMAP.md:
1. At call start, the agent correctly names each habit due today and knows which are already marked complete
2. When the user says a habit is done, Habitify reflects completed status immediately after the call
3. For goal-based habits (e.g., "ran 5km"), Habitify logs the numeric value — not just a yes/no status
4. Habits the user says they skipped are correctly left as incomplete in Habitify
5. The agent handles at least 5 habits in a single call without tool-step limits blocking completion
</verification>

<success_criteria>
Live phone call with real Habitify data passes all 5 success criteria. Pre-call briefing contains accurate habit analysis. Habitify reflects correct post-call state.
</success_criteria>

<output>
After completion, create `.planning/phases/03-habitify-integration/03-03-SUMMARY.md`
</output>
