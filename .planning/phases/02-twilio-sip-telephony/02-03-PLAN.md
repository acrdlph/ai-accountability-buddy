---
phase: 02-twilio-sip-telephony
plan: 03
type: execute
wave: 3
depends_on: [02-01, 02-02]
files_modified: []
autonomous: false
requirements: [FR1, FR8, NFR4]

must_haves:
  truths:
    - "Dispatching the agent with a phone number rings the target phone within 10 seconds"
    - "Answering the call connects to the agent, which speaks first"
    - "If the call goes unanswered, the agent process exits cleanly with a logged TwirpError"
    - "Single hardcoded phone number works via DEFAULT_PHONE_NUMBER env var when metadata is empty"
  artifacts: []
  key_links:
    - from: "lk dispatch create"
      to: "agent.py entrypoint"
      via: "LiveKit dispatch with metadata"
      pattern: "dispatch create.*agent-name accountability-buddy"
---

<objective>
End-to-end test: manual CLI dispatch rings phone, agent speaks first, no-answer path exits cleanly.

Purpose: Validates the entire SIP telephony chain from dispatch through Twilio to a real phone. This is the proof that Phase 2 works.
Output: Verified working outbound calling with all three paths tested (answered, no-answer, voicemail).
</objective>

<execution_context>
@/Users/achill/.claude/get-shit-done/workflows/execute-plan.md
@/Users/achill/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-twilio-sip-telephony/02-RESEARCH.md
@agent.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Start agent worker and dispatch test call</name>
  <files></files>
  <action>
Start the agent and dispatch a test call to verify the SIP chain works end-to-end.

**1. Start the agent worker** in a terminal:
```bash
python agent.py dev
```
Confirm it connects to LiveKit Cloud without errors.

**2. In a second terminal, dispatch a call** with the user's phone number in metadata:
```bash
lk dispatch create \
  --new-room \
  --agent-name accountability-buddy \
  --metadata '{"phone":"<DEFAULT_PHONE_NUMBER_FROM_ENV>"}'
```

**3. Watch agent logs** for:
- `"SIP call failed"` -- indicates trunk misconfiguration (go back to Plan 01)
- Successful participant join -- indicates the call connected
- `"Voicemail detected"` -- indicates voicemail tool fired

**If the call fails with TwirpError:**
- SIP 401/403: Credential-list not associated with trunk in Twilio (see Research pitfall 1)
- SIP 502/503: Wrong trunk address format (see Research pitfall 2)
- Connection timeout: Check that `lk sip outbound list` shows the trunk

**4. Also test with empty metadata** (falls back to DEFAULT_PHONE_NUMBER):
```bash
lk dispatch create \
  --new-room \
  --agent-name accountability-buddy \
  --metadata '{}'
```
  </action>
  <verify>
    <automated>python agent.py dev 2>&1 | head -5 | grep -q "connected" && echo "PASS" || echo "Agent starts -- verify manually"</automated>
  </verify>
  <done>Agent worker starts, dispatch triggers SIP call, phone rings (or error is diagnosed)</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Verify answered call, no-answer, and voicemail paths</name>
  <action>Human verification of the complete SIP telephony chain across all call scenarios.</action>
  <what-built>Complete SIP telephony chain: agent dispatches outbound call via Twilio, handles answered/unanswered/voicemail scenarios</what-built>
  <how-to-verify>
Test all three call paths:

**Test 1 -- Answered call (happy path):**
1. Start `python agent.py dev` in one terminal
2. Run: `lk dispatch create --new-room --agent-name accountability-buddy --metadata '{"phone":"+1YOUR_NUMBER"}'`
3. Answer your phone when it rings
4. Verify: Agent speaks first within 1-2 seconds of answering (tough-love greeting)
5. Have a brief conversation, then let the agent end the call
6. Verify: Call ends cleanly, no errors in agent logs

**Test 2 -- No-answer path:**
1. With agent still running, dispatch another call
2. Do NOT answer the phone -- let it ring until it stops
3. Verify: Agent logs show `"SIP call failed"` with a SIP status code (480 = no answer)
4. Verify: Agent process does NOT crash -- it logs the error and shuts down the job cleanly

**Test 3 -- Voicemail path (if possible):**
1. If you can route a call to voicemail (e.g., decline the call), do so
2. Verify: Agent logs show `"Voicemail detected -- hanging up without leaving a message"`
3. Verify: No voicemail is left on the phone

**Test 4 -- Env var fallback:**
1. Dispatch with empty metadata: `lk dispatch create --new-room --agent-name accountability-buddy --metadata '{}'`
2. Verify: Phone still rings (DEFAULT_PHONE_NUMBER env var used)
  </how-to-verify>
  <resume-signal>Type "all tests pass" or describe which test failed and what you observed</resume-signal>
</task>

</tasks>

<verification>
- Dispatching with phone metadata rings the target phone
- Answering connects to the agent, which speaks first
- Not answering causes clean shutdown with TwirpError logged
- Empty metadata falls back to DEFAULT_PHONE_NUMBER
- Voicemail detection works (if testable)
</verification>

<success_criteria>
All Phase 2 success criteria from ROADMAP.md are verified:
1. `lk dispatch create` rings target phone within 10 seconds
2. Answering connects to agent, which speaks first
3. Unanswered call exits cleanly (TwirpError caught)
4. Voicemail detected and agent hangs up (if testable)
5. DEFAULT_PHONE_NUMBER env var works as fallback
</success_criteria>

<output>
After completion, create `.planning/phases/02-twilio-sip-telephony/02-03-SUMMARY.md`
</output>
